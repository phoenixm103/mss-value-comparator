apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  labels:
    build: valuecomparator-build
  name: valuecomparator-build
  namespace: valuecomparator
spec:
  failedBuildsHistoryLimit: 5
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: 'valuecomparator:latest'
  postCommit: {}
  resources: {}
  runPolicy: Serial
  source:
    type: Binary
  strategy:
    dockerStrategy:
      from:
        kind: ImageStreamTag
        name: 'openjdk:8-jdk-alpine'
    type: Docker