apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  labels:
    build: value-comparator-build
  name: value-comparator-build
  namespace: value-comparator
spec:
  failedBuildsHistoryLimit: 5
  nodeSelector: null
  output:
    to:
      kind: ImageStreamTag
      name: 'value-comparator:latest'
  postCommit: {}
  resources: {}
  runPolicy: Serial
  source:
    type: Binary
  strategy:
    dockerStrategy:
      from:
        kind: ImageStreamTag
        name: 'openjdk:8-jdk-alpine'
    type: Docker