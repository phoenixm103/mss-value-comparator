apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: valuecomparator
  name: valuecomparator
  namespace: valuecomparator
spec:
  host: valuecomparator-maxis.nagadpay.com 
  port:
    targetPort: 6091-tcp
  to:
    kind: Service
    name: valuecomparator
    weight: 100
  wildcardPolicy: None